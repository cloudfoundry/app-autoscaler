{
  "name": "auto-scaler-service-broker",
  "description": "autoscaler-service-broker",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "bin": {
    "mocha": "./bin/mocha",
    "_mocha": "./bin/_mocha"
  },
  "dependencies": {
    "eslint": "^2.11.1",
    "basic-auth": "^1.0.4",
    "body-parser": "^1.15.1",
    "express": "^4.13.4"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.5.3",
    "should": "^9.0.0",
    "supertest": "^1.2.0"
  },
  "scripts": {
    "bootstrap": "npm install eslint mocha -g && npm install && npm install bin/cfpack",
    "lint": "eslint lib",
    "start": "node lib/index.js",
    "test": "mocha test --recursive",
    "watch-test": "mocha test --recursive --watch",
    "package": "./bin/cfpack/cfpack"
  }
}
