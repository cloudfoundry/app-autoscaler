cf:
  api: "https://api.example.com"
  grant_type: "client_credentials"
  client_id: "cf_id"
  secret: "cf_secret"
server:
  port: 8080
logging:
  level: "debug"
db:
  policy_db_url: "postgres://postgres:postgres@localhost/autoscaler" 
  metrics_db_url: "postgres://postgres:postgres@localhost/autoscaler"
collector:
  refresh_interval: 60
  poll_interval: 10
  
