logging:
  level: "debug"
metrics_db:
  db_url: "postgres://postgres:postgres@localhost/autoscaler?sslmode=disable"
  refresh_interval_in_hours: 12
  cutoff_days: 30
app_metrics_db:
  db_url: "postgres://postgres:postgres@localhost/autoscaler?sslmode=disable"
  refresh_interval_in_hours: 12
  cutoff_days: 30
