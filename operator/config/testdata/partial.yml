app_metrics_db:
  db:
    url: "postgres://postgres:postgres@localhost/autoscaler?sslmode=disable"
scaling_engine_db:
  db:
    url: "postgres://postgres:postgres@localhost/autoscaler?sslmode=disable"
app_syncer:
  db:
    url: "postgres://postgres:postgres@localhost/autoscaler?sslmode=disable"
