logging:
  level: "debug"
instance_metrics_db:
  db_url: "postgres://postgres:postgres@localhost/autoscaler?sslmode=disable"
  refresh_interval: 12h
  cutoff_days: 30
app_metrics_db:
  db_url: "postgres://postgres:postgres@localhost/autoscaler?sslmode=disable"
  refresh_interval: 12h
  cutoff_days: 30
